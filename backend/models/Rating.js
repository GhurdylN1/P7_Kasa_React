// systeme de notation par étoiles a faire, qui doit récuperer les notations des locataires et faire une moyenne, besoin d'un model ? 

const mongoose = require('mongoose');

const ratingSchema = mongooseSchema ({
    
});

module.exports = mongoose.model('Rating', ratingSchema);